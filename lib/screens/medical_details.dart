import 'package:flutter/material.dart';

class MedicalDetails extends StatelessWidget {
  const MedicalDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Medical Details');
  }
}